// Extends the conventional commits standard — this recognizes types like
// feat, fix, chore, docs, test, refactor, style, perf, ci
module.exports = {
  extends: ['@commitlint/config-conventional']
}